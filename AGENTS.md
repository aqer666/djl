# AGENTS.md

本文件是 OC Forge / 二次元 AI 创作与周边平台的项目级 agent 指南。所有代码代理在修改本仓库前必须先阅读本文件，并以商业计划书和现有网站雏形为准。

## 项目定位

OC Forge 的主定位是面向二次元用户的 AI 角色创作与商业化工具平台，帮助用户把 OC 灵感转化为可保存、可展示、可传播、可消费的角色资产。

不要把项目做成泛二次元社区、AI 恋爱平台、完整商城、原生 App、自研大模型或自建供应链。情感陪伴只能作为角色创作辅助体验，不作为主定位。

## 当前仓库现状

- 当前是静态前端 MVP：`index.html`、`styles.css`、`app.js`。
- `app.js` 内含 mock 数据、hash 路由、本地状态、画师市场、OC 档案、约稿订单、公共稿单、社区晒图和后台审核交互。
- `server.mjs` 是本地演示后端，提供静态资源托管和 `POST /api/images/generate` 图片生成接口。
- 未配置真实 API Key 时，图片生成走本地 mock SVG；配置 `OPENAI_API_KEY` 或 `REPLICATE_API_TOKEN` 后走真实 provider。
- `二次元 AI 创作与周边平台商业计划书.pdf` 是产品和商业路线依据。

## 产品主线

第一阶段优先验证以下闭环：

1. 用户输入 OC 灵感。
2. AI 生成完整角色设定。
3. 用户选择风格生成头像。
4. 系统生成角色卡。
5. 用户继续生成同人剧情或角色对话风格。
6. 用户导出角色卡或头像套装。
7. 系统引导高清导出、会员、点数、周边预览或摊宣服务。

现有约稿/画师/订单流程可以作为后续商业化和创作者生态模块保留，但不要让它抢占 MVP 的首要闭环。

## 目标用户优先级

优先服务：

- OC 创作者：人设、头像、角色卡、世界观。
- 同人作者：剧情、大纲、对话、CP 互动。
- 漫展摊主：摊宣文案、商品菜单、价格牌、周边预览。

暂缓泛娱乐用户、完整社交、复杂 IM、复杂商城、库存管理和多人协作。

## 开发路线图

### 阶段 0：Demo 整理与验证准备

目标：把现有雏形整理成可演示、可访谈、可记录反馈的 Demo。

- 保持静态原型可运行。
- 明确当前 mock 流程与未来真实数据模型的对应关系。
- 补齐 OC 生成、角色卡、头像生成、导出入口的演示链路。
- 保留画师约稿、公共稿单和后台审核作为后续参考模块。
- 用 20-50 个种子用户测试兴趣和理解成本。

关键指标：

- 单用户平均生成次数 >= 5。
- 角色保存率 >= 30%。
- 导出点击率 >= 15%。
- 至少 10 人明确表达付费意愿。

### 阶段 1：0-1 个月验证期

目标：完成可用 MVP Demo，验证 OC 创作闭环。

- 用户注册 / 登录。
- OC 人设生成。
- 角色资产保存。
- 角色卡生成与导出。
- 基础头像生成。
- 同人剧情片段生成。
- 生成次数、保存率、导出点击、反馈记录。

不要做完整社区、私信系统、完整商城、自建供应链或大规模 AI 陪伴聊天。

### 阶段 2：2-3 个月收费 MVP

目标：上线可收费版本。

- 接入会员和点数系统。
- 接入支付。
- 增加高级模板。
- 增加摊宣图文生成。
- 增加周边预览。
- 支持高清导出。
- 冷启动渠道优先小红书、B站、微博、QQ 群。

关键指标：

- 注册用户 1000-3000。
- 日活用户 100-300。
- 付费转化率 2%-5%。
- 月收入 3000-15000 元。
- 用户次月留存 >= 20%。

### 阶段 3：4-6 个月商业化增强

目标：验证持续收入和创作者生态。

- 创作者模板市场。
- 周边下单流程，早期优先人工对接供应商。
- 摊主工具增强。
- 漫展、社团、画师合作。
- 内容安全和版权审核机制。
- 基础运营后台和数据看板。

## 推荐生产技术栈

当前原型可以继续用原生 HTML/CSS/JS 快速验证；进入生产化重构时使用以下栈：

- 前端：Next.js App Router + React + TypeScript。
- 样式：Tailwind CSS + shadcn/ui + lucide-react；保留当前偏工作台的深色视觉方向，但组件要系统化。
- 后端：Next.js Route Handlers / Server Actions 起步；只有当业务复杂度明显上升时再拆独立服务。
- 数据库：PostgreSQL + Prisma。
- 认证：Auth.js、Clerk 或 Supabase Auth，按部署和支付环境选择其一。
- 文件存储：S3 兼容对象存储，如 Cloudflare R2、AWS S3 或 Supabase Storage。
- 缓存 / 队列：Redis / Upstash；用于图片生成任务、点数扣减幂等和异步轮询。
- AI 文本：通过 provider adapter 封装 OpenAI、兼容 OpenAI API 的模型或国内模型。
- AI 图片：保留 OpenAI Images / Replicate provider adapter；不要把 provider 调用散落在业务组件里。
- 支付：早期可先抽象 payment provider；国内上线优先评估微信支付 / 支付宝，海外版本再评估 Stripe。
- 部署：Vercel 可作为首选；如需国内访问和支付合规，另行评估国内云。

## 核心数据模型

生产化时至少规划这些表或模型：

- `users`：账号、角色、状态。
- `profiles`：昵称、头像、用户分层、会员状态。
- `characters`：OC 资产、设定、可见性、标签、版本。
- `character_generations`：人设、剧情、头像、角色卡等生成记录。
- `assets`：图片、角色卡、导出文件、AIGC 标识元数据。
- `templates`：角色卡、剧情、头像、摊宣、价格牌模板。
- `points_ledger`：点数发放、消耗、退款、幂等键。
- `memberships`：会员计划、权益、周期。
- `orders`：高清导出、周边设计包、实体周边或后续约稿订单。
- `payments`：支付状态、provider、金额、回调验签结果。
- `posts`：后续社区展示内容。
- `reports`：举报、版权投诉、审核结果。
- `audit_logs`：后台操作和高风险状态变更。

现有原型中的 `artists`、`services`、`publicJobs`、`orderReviews` 可作为后续画师市场模块的数据模型参考，不是第一阶段必需表。

## 合规与安全边界

- 所有 AI 生成内容页面必须提示“由 AI 生成 / 辅助生成”。
- 导出图片和文件需要保留适当 AIGC 标识；不要提供移除标识功能。
- 禁止知名 IP 复刻、仿画师风格、现实人物冒充、未授权商业周边。
- 周边和商用导出必须要求用户确认原创或已获授权。
- 不向未成年人提供虚拟伴侣、虚拟亲密关系或高依赖情感陪伴功能。
- 禁止色情低俗、未成年人不当内容、暴力血腥、仇恨歧视、违法交易、自伤诱导。
- Prompt 和上传内容要经过基础安全检查；高风险内容进入人工审核或拒绝。
- 不要把 API Key、支付密钥、数据库连接串写入代码、README、日志或示例输出。

## 代码规范

- 优先小改动、可审查 diff，不做无关重构。
- 不臆造 API、配置或路径；不确定时先用 `rg --files`、`rg` 或阅读现有代码。
- 新功能优先建立明确边界：UI、业务逻辑、provider adapter、数据访问分层。
- TypeScript 代码必须显式建模核心业务状态，不用 `any` 绕过。
- 表单和接口必须有输入校验、错误处理、加载态和空状态。
- 不在前端暴露密钥、provider token 或支付回调验证逻辑。
- 不新增 analytics、telemetry 或外部网络调用，除非用户明确要求。
- 注释只解释非显然意图，不复述代码。
- 中文界面文案保持简洁、明确、符合二次元创作工具语境。

## 前端体验规范

- 第一屏必须是可用产品界面，不做纯营销落地页。
- 面向创作工具和运营后台的页面要信息密度适中，避免过度装饰。
- 角色卡、头像、摊宣、周边预览必须展示真实结果或清晰 mock，不用纯占位块。
- 控件要符合预期：模式用分段控件，开关用 toggle，数字用 input/stepper，操作按钮使用图标加必要文字。
- 移动端和桌面端都不能出现文字溢出、按钮挤压、内容互相遮挡。
- 保持稳定尺寸，避免 hover、加载态或长文案导致布局跳动。

## 测试与验证

当前静态原型的最快检查：

```bash
node server.mjs
```

然后访问：

```text
http://127.0.0.1:8765/
```

生产化后应至少包含：

- 单元测试：业务状态转换、点数扣减、provider adapter、权限判断。
- 组件测试：核心表单、角色卡、生成结果、支付入口。
- E2E 测试：注册登录、OC 生成、头像生成、角色卡导出、点数扣减、支付回调模拟。
- 安全测试：Prompt 拦截、未授权访问、支付回调验签、对象存储访问控制。

每次可能影响运行的改动，先跑最快相关检查，再跑更完整的验证。

## Skills 使用规则

当任务匹配技能时，必须先读取并遵循对应 `SKILL.md`：

- 新功能、行为变更、产品方案：使用 `superpowers:brainstorming`。
- 多步骤实现计划：使用 `superpowers:writing-plans`。
- 按计划执行：使用 `superpowers:executing-plans` 或 `superpowers:subagent-driven-development`。
- 调试 bug 或异常：使用 `superpowers:systematic-debugging`。
- 重要功能完成前：使用 `superpowers:verification-before-completion`。
- 前端页面或交互设计：使用 `frontend-design`，并遵守本文件的前端体验规范。
- PDF 读取、生成或验证：使用 `pdf` 技能。
- OpenAI / Codex / API 相关问题：使用 `openai-docs`，并优先查官方文档。
- GitHub PR、CI、评论处理：使用 `github:*` 对应技能。
- 部署到 Vercel：使用 `vercel-deploy` 或相关 Vercel 技能。

技能说明高于默认工作习惯，但不能覆盖用户明确指令、安全边界和本文件中的项目约束。

## Agent 工作流程

1. 修改前先说明会改哪些文件和 3-6 条计划。
2. 先读现有代码和文档，再提出实现方式。
3. 保持 diff 小而聚焦，不顺手重写无关模块。
4. 如果发现用户已有改动，不要回滚；必须基于现状继续。
5. 涉及密钥时只要求用户通过环境变量提供。
6. 完成后说明改动摘要、涉及文件、验证命令和结果。
7. 如果无法验证，明确说明原因和建议的本地命令。

## 近期优先级

下一轮开发建议按此顺序推进：

1. 把现有原型中的约稿主线调整为商业计划书中的 OC 创作主线。
2. 增加 OC 人设生成、剧情片段生成、角色对话风格生成的前端流程。
3. 将头像生成结果绑定到角色资产。
4. 增加角色卡生成和导出演示。
5. 增加点数消耗的 mock ledger，为真实支付和会员做准备。
6. 增加 AIGC 标识、版权确认和基础内容安全拦截。
7. 再推进摊宣模板、周边预览和画师市场。
